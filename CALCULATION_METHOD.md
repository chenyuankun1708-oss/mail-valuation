# 收益计算口径与重要决策

## 公式展示规范

网页“计算口径”对每个可计算指标同时展示公式、变量含义、单位、年化方式、最低样本和缺失规则。核心公式包括：

- 收益金额：`P = A1 - A0 - S + R + D`。
- XIRR：求解 `Σ CFi / (1+r)^((ti-t0)/365) = 0`。
- 净值CAGR：`(NAV1/NAV0)^(365/d) - 1`。
- 年化波动率：不规则估值日先计算 `ri = ln(NAVi/NAVi-1)/Δdi`，按间隔天数加权后乘 `√365`；交易日指数日收益标准差乘 `√252`。
- 夏普率：`(年化对数收益 - 2%) / 年化波动率`。
- 最大回撤：`min(NAVt / max(NAVs, s≤t) - 1)`；Calmar为`CAGR / |最大回撤|`。
- Beta为加权协方差除以基准加权方差，Alpha按固定2%无风险利率的CAPM日收益差年化。
- 10年期国债收益率VaR取`(yt-yt-1)×10000` bp的右尾分位；价格指数和碳价取下跌方向分位。
- 固收金额风险近似为`敞口 × 代理久期 × 收益率或利差冲击`；美元资产另加未对冲汇率分量，所有分量线性相加。

产品分组、标签匹配、日期边界、数据来源和缓存回退属于规则口径，不伪造成公式。

## 文档与变更记录维护

凡是影响计算结果、数据解释、用户操作或页面行为的重要改动，必须同时更新网页“数据与说明”、网页“历史改动”、`AGENTS.md`、`README.md`及相关专项说明、`logs/CHANGELOG.md`。版本变更日志不得包含凭据；刷新、隧道和分享服务等本机运行日志继续由Git忽略。

运行计时不参与任何收益或风险计算。所有CLI耗时使用 `time.perf_counter()` 单调时钟观测；JSON秒数保留三位，控制台使用一位小数的易读格式。`refresh`的邮件、行情缓存、数据计算和网页写入分别计时，分类步骤互斥汇总，总耗时按命令起止直接测量，不以分类值重复相加。

标签数据已由《产品标签.xlsx》和《管理人清单.xlsx》一次性迁移到 `data_sources/product_labels.json`。JSON保存稳定产品标识、来源记录标识、管理人、一二级标签、投资形式、分类依据、启用状态、记录版本及更新时间；迁移后它是唯一运行时权威源，原Excel仅作历史备份。名称匹配与“其他”回退规则不变。

标签查询、创建、修改和停用接口沿用Basic Auth。写入以全局修订号执行乐观并发控制：请求修订号与当前值不一致时返回冲突，不覆盖新数据。每次成功修改先保存上版备份，再以临时文件加 `os.replace` 原子发布；审计日志只记录时间、登录用户名、产品、动作、修订号和修改前后字段。

网页标签编辑器保存后会重新请求服务端依据当前JSON计算的完整底层匹配结果，并在浏览器内重新执行收益分析、策略组合和FOF1专户/策略分类；这属于当前会话即时刷新，不依赖重新生成静态网页。未匹配和歧义项仍显著显示并归入“其他”。

核心汇报采用确定性触发规则：单产品正/负贡献达到全部产品绝对收益变动的10%，前三大产品期末资产占比达到50%，策略占比绝对变化达到5个百分点，最大回撤不高于-10%，或年化波动率达到30%时进入候选重点；按严重程度取前三至五项。数据完整性提示不进入核心汇报，统一在“数据与说明”诊断。资产勾稽为 `期末资产 - (期初资产 + 申购 - 赎回 - 现金分红 + 收益)`；核心汇报不读取“其他管理”的风控日报VaR。

核心汇报页面沿用顶部日期和范围，以分段核心结论开头，再按“摘要→重点事项→证据明细”展示。全部顶层产品申购赎回逐笔完整读取资金台账；底层范围仅限盛乾同行2号FOF、源泉优享FOF3号、天玑13号和西南证券嘉盈1号FOF。底层持仓没有真实成交流水，存续持仓增减金额按 `(期末份额－期初份额)×期末估值价格` 估算，新增按期末市值、退出按负的期初市值。底层估值价格代理收益率为 `期末价格÷期初价格－1`；一级策略收益率为可计算底层收益率按期初市值加权。旧版“生成报告”按钮不再展示，统一由核心汇报下载Excel或打印PDF。

## 底层证券多因子分析

底层历史估值表只读取股票叶子科目，汇总证券代码、市值和权重。因子使用估值日前最近可用的Wind `QUANTFACTORWIND`数据；同日A股截面按1%和99%分位缩尾，再转换为标准分。产品因子暴露为证券标准分按股票市值权重的加权和。

当前九类因子为规模、价值、动量、波动、流动性、成长、盈利、杠杆和股息。行业使用Wind行业分类的生效日、失效日进行时点匹配。本模块不包含正式Barra模型所需的因子协方差矩阵和证券特异风险，因此不计算正式因子风险贡献、跟踪误差或预测风险。

## 本地知识库规则口径

知识库不计算投资指标。文件以原始字节的SHA-256作为去重标识，原文件与SQLite元数据分离保存。DOCX/PPTX读取其Open XML正文，XLS/XLSX读取可见单元格值，TXT/Markdown/HTML读取本地文本，PDF首版只提取已有文本层；空文本明确标为需OCR。检索覆盖标题、原文件名、正文、标签、产品和机构。停用文档移入回收区，不直接物理删除；网页只接受上传内容、固定文档ID和固定inbox导入，不接受任意路径。

## 持仓变动归因（估算）

相邻估值日的底层持仓市值变化按下式拆分：

`ΔMV = q0 × (P1 - P0) + (q1 - q0) × P1`

其中，`q0、q1`分别为期初、期末份额，`P0、P1`分别为期初、期末估值价格。第一项是原持仓价格影响，第二项是按期末估值价格计算的仓位变化金额，单位均为人民币元。两端份额和价格完整时标记“可归属”；新增持仓按期末市值、清仓持仓按负的期初市值估算并标记“部分可归属”；参数缺失时标记“不可归属”，不反推成交信息。

产品未归属差额＝顶层产品区间收益－该产品底层可归属价格影响。该差额可能包含现金、费用、实际交易价格、退出损益及其他未穿透项目。仓位变化金额仅描述持仓规模变化，不是交易盈利；本模块不是基于真实成交流水的正式交易归因。

## 策略开发实验室计算口径

策略实验室只消费固定白名单本地缓存，并为每个因子样本保存 `available_at`。月末信号日为 `t`，预测目标为下一月指数相对沪深300的超额收益：`y(i,t+1)=R(i,t+1)-R(300,t+1)`。

特征包括1/3/6/12月动量、60日年化波动率、相对沪深300的3/6月强弱，以及截至 `t` 已发布的宏观、风格和情绪状态。历史月度宏观值以实际发布日期进入样本，不按统计期提前使用。Ridge只使用 `target_end ≤ t` 的已实现目标，且训练月数不少于36；目标函数为 `min Σ(y-Xβ)² + λΣβj²`，截距不惩罚，均值和标准差只在当期扩展训练窗内计算。规则基线先在每个信号日横截面标准化并截断至±3，再按趋势35%、宏观20%、相对强弱20%、情绪与波动15%、风格与估值10%加权。

正预测品种按 `max(预测值, 0.01) ÷ 60日波动率` 形成风险预算倾斜；宽基为保证组合可投资性始终进入候选。组合100%投入、无杠杆、无做空，宽基合计不低于50%，风格和行业各不超过30%，单只宽基/风格/行业分别不超过35%/15%/10%。单边换手为 `0.5×Σ|w目标－w上期|`，超过30%时沿上期权重按比例收缩。交易成本从当月模拟收益中扣除 `单边换手×成本bp÷10000`，分别报告0、10、20bp。

回测采用扩展窗口逐月走步，信号在已结束月份的最后交易日收盘后形成，未结束的当月数据不得冒充月末信号；下一交易日开始模拟持有至下一期。最新建议使用最近已结束月份的特征重新拟合，不以最后一个已有实现收益的回测期代替。结果记录训练区间、样本数、系数、特征贡献、约束检查、无前视检查、方向命中率和相对规则基线的表现。ETF只可从信号日已上市至少120个交易日的获批候选中选择；缺失时保留指数信号并明确不可执行，不以当前ETF信息回填历史。结果仅供研究，不构成投资指令。

网页日期框只列出已生成的因子快照。选择某一快照后，展示该快照的实际持仓估值日，并向前匹配最近Wind因子日，禁止使用未来因子。Wind行业分类表的10位代码与行业字典部分16位补零代码按等价规则连接。

## 适用前提

本项目中的17只单一资产管理计划，每只产品有且仅有一个客户。估值表中的资产净值因此视为该客户在产品中的全部持仓市值。

## 当前正式口径

区间收益金额使用：

```text
收益 = 期末资产净值 - 期初资产净值 - 申购金额 + 赎回金额 + 现金分红
     = 期末资产净值 - 期初资产净值 - 台账净现金流 + 现金分红
```

台账现金流约定：申购为正数，赎回为负数。现金分红若已导致资产净值下降，需要加回收益；当前根据累计单位净值与单位净值差额的变化推定，仍应以公告或银行流水核验。

年化收益率使用 XIRR。现金流由期初资产（负数）、区间申赎、现金分红和期末资产（正数）组成。

## 累计净值收益与风险指标

产品详情保留资金加权XIRR，并另外使用所选区间内的真实累计单位净值计算净值表现：

```text
净值年化收益率 = (期末累计净值 / 期初累计净值) ^ (365 / 实际天数) - 1
日对数收益 = ln(本期累计净值 / 上期累计净值) / 相邻估值间隔天数
年化波动率 = 按间隔天数加权的日对数收益标准差 × √365
夏普率 = (日对数收益均值 × 365 - ln(1 + 2%)) / 年化波动率
最大回撤 = min(当前累计净值 / 此前累计净值峰值 - 1)
```

收益分析将计算起点的资产净值作为“期初资产”单独列示；区间内首次建仓时该值为0。“最大回撤区间”紧随最大回撤展示，起点为回撤前累计净值峰值日，终点为对应谷值日，格式统一为 `YYYY-MM-DD → YYYY-MM-DD`；样本不足时显示“无法计算”，不另设重复说明文字。

程序不补齐或虚构缺失交易日。净值年化和最大回撤至少需要2个有效估值点，年化波动率和夏普率至少需要3个；波动率为0时夏普率不计算。区间少于30天仍展示结果，但网页提示短区间年化指标不稳定。区间内首次建仓产品只使用建仓后的真实累计净值点，不把期初资产为0作为净值起点。砚博仅有天玑13号披露的底层价格，没有正式累计单位净值，因此不计算上述风险指标。

## Wind指数基准与Alpha/Beta

生成网页固定嵌入2010-01-01以来的指数行情；如产品最早估值日更早，则再向前保留。指数同期表现可以选择早于现有FOF成立日期的区间独立计算，不受FOF估值表起始日期限制。

顶部同期表现仅展示中证1000（000852.SH）、中证500（000905.SH）、沪深300（000300.SH）和中证2000（932000.CSI），均读取 `AIndexEODPrices`。南华商品指数如存在，仅保留给底层CTA标签基准及风控内部计算，不进入顶部同期表现。所选边界不是交易日时，向前取最近交易日收盘：

```text
区间涨跌 = 期末指数收盘 / 期初指数收盘 - 1
指数CAGR = (期末指数收盘 / 期初指数收盘) ^ (365 / 实际天数) - 1
指数最大回撤 = min(当前收盘 / 此前收盘峰值 - 1)
```

每只顶层产品相对中证1000计算Alpha/Beta。产品相邻真实累计净值点形成一个回归区间，指数在相同起止日期分别向前匹配最近收盘；双方均以区间实际日历天数换算日对数收益，并以该区间天数为权重：

```text
Beta = 加权协方差(中证1000日对数收益, 产品日对数收益) / 中证1000加权方差
日Alpha = 产品加权日收益均值 - [日无风险收益 + Beta × (基准加权日收益均值 - 日无风险收益)]
年化Alpha = exp(日Alpha × 365) - 1
日无风险收益 = ln(1 + 2%) / 365
```

至少需要10个有效相邻区间、实际跨度不少于30天且基准方差非零。指标不用于分组或砚博。对比曲线将所有勾选产品的累计净值和三只默认指数分别按各自实际起点归一化为100；指数缺失日只向前取最近收盘，不虚构产品净值点。当前详情产品和中证1000、中证500、沪深300默认勾选，其他顶层产品默认不选；切换详情产品后重置默认选择。Wind查询失败时保留上次成功缓存；同期表现中无数据库记录的指数显示“暂无行情”，最新行情超过10天则提示可能过期。网页标题下同时显示该HTML的生成更新时间；结果仅供分析参考。

## 为什么不使用反推份额计算收益

部分估值表无法可靠提取真实总份额，解析器可能使用：

```text
反推份额 = 资产净值 / 单位净值
```

曾尝试按下式计算收益：

```text
收益 = Σ[上一估值日份额 ×（本期累计单位净值 - 上期累计单位净值）]
```

这一方案已否决。若份额是反推值而非估值表原始值，单位净值变化会自动造成反推份额变化，程序会把这种数学变化误认为真实持仓份额，进而虚增盈利或亏损。

天玑13号的2026年7月数据暴露了该问题：资产净值口径亏损约2,823.95万元，而反推份额口径一度计算为约7,295.58万元，多计亏损约4,471.63万元。反推份额今后只能用于数据核验，不能作为收益金额的计算依据。

## 多列估值表的市值选择

部分估值表在同一“基金资产净值”行同时列示成本口径、成本占比、市值口径、市值占比和估值增值。不能简单选择其中最大数值。程序先读取估值表披露的实收资本/总份额，再以 `真实份额 × 单位净值` 为参照，从该行选择最接近的资产净值列。

国泰海通私客尊享FOF8075号的2026年7月估值表曾暴露此问题：程序错误选择51,948,971.80元，正确市值为49,395,532.71元。修正后保留估值表披露的50,000,000份，不再用错误资产净值反推份额。

## 日期边界限制

估值日期优先读取表头中的“估值日期/日期”，再回退到文件名。年月日正则优先匹配两位月份及两位日期，避免将 `2025-12-30` 截断为 `2025-01-02`；原始估值表不改写。

若所选结束日期早于某产品首个真实估值日，该产品在该历史区间属于“尚未成立”，不计入“已计算产品”的分子或分母，也不产生该区间的资产和收益。产品仍保留在网页列表中，并明确显示“所选结束日期时产品尚未成立”。

若台账确认产品在所选区间内首次建仓，则期初资产按0、首次建仓日起纳入申赎；“已计算产品”只要求结束边界存在有效估值表，不要求成立前存在期初估值表。该放宽仅适用于区间内新成立产品；台账显示开始日前已经建仓但缺少期初估值的产品仍不计算。

收益公式本身依赖台账现金流与估值资产落在同一统计区间。如果申赎在台账中的登记日期与估值表确认日期相差数日，尤其跨越月初或月末，月度收益会发生跨期偏差。

当前页面选择边界当天没有估值表时，会使用该日期之前最近的估值表，并显示边界提示。遇到边界附近的大额申赎，应核对交易确认日期、银行流水和相邻估值表；不要通过反推份额规避该问题。

## 区间内新成立或首次投资

产品在所选开始日期之前没有估值表，但在区间内已经有估值表和台账投资记录时，仍然可以计算收益。此时按以下规则处理：

```text
期初资产净值 = 0
收益 = 区间末资产净值 - 区间内申购 + 区间内赎回 + 现金分红
```

区间现金流从用户选择的开始日期（含当日）统计至最后估值日。页面会明确标记“区间内新成立/首次投资”，不会要求虚构一张成立前估值表。例如中信建投聚智多策略9号在2026年6月中首次投资，6月收益可用6月投入和6月30日估值计算。

## 2026年7月天玑13号示例

```text
期初资产净值：923,791,791.13元
期末资产净值：895,552,260.42元
申购：10,000,000.00元
赎回：10,000,000.00元
净现金流：0.00元
收益：895,552,260.42 - 923,791,791.13 = -28,239,530.71元
```

## FOF底层产品收益分析

估值表能识别底层产品的科目代码、名称、数量、单位成本、成本、市价、市值和估值增值时，网页展示：

- 收益率贡献：`底层期间估算收益 ÷ FOF区间首个真实估值日资产净值`。柱状图红色为正贡献、绿色为负贡献，鼠标悬浮显示底层名称、期间估算收益和贡献率。该贡献率是持仓可归属估算，不代表完整业绩归因。

- 当前浮盈亏：直接采用估值表的估值增值，缺失时使用 `市值 - 成本`。
- 期间估算收益：相邻估值日均持有时，按 `两期较小持仓数量 ×（本期价格 - 上期价格）` 计算，避免把新增买入的市值当作盈利。
- 区间新增持仓：没有上期价格时，按当前浮盈亏作为入场以来估算收益。
- 区间退出持仓：估值表没有成交价格，无法可靠归属退出收益，网页明确标记而不猜测。

底层期间收益属于持仓层面的可归属估算，不包含现金、管理费、托管费、业绩报酬以及无法归属到具体产品的已实现损益，因此不要求与FOF整体区间收益完全相等。仅披露汇总净值报告、没有持仓科目明细的产品无法展示底层分析。

## 底层标签、净值代理与策略组合

公网分享不改变任何收益计算。Windows开机任务以最高权限运行Tailscale恢复脚本，仅用于访问Tailscale受保护的本地管道；Python网页仍只监听`127.0.0.1`并保留Basic Auth。

Tailscale重启后若暂处于`NoState`，分享脚本只会启动官方桌面客户端并等待网络进入Running，再恢复Funnel；该恢复过程不改变网页认证、固定域名或任何投资计算结果。

网页传输层也不改变计算口径：本机构建阶段完成数据库读取和计算，访客浏览器只读取生成后的JSON。首屏与按页面拆分的固定白名单模块均受Basic Auth保护并使用gzip、ETag及私有缓存；市场研究、多因子、底层资产、台账/标签工作簿和风控日报VaR在进入相关页面时加载，不允许通过接口指定任意本机文件。

入口加载`app.js`时使用本次构建的`page_updated_at`作为版本参数，脚本缓存要求重新校验ETag，确保网页重建后的交互与本次数据文件保持一致。

单日估值表压缩包不是计算结果，也不采用估值边界向前取值：仅选择估值日期严格等于请求日的已配置顶层产品原始文件。扫描阶段先按SHA-256排除内容重复附件；同一产品同一日期存在多个非重复版本时，沿用解析规则保留底层持仓更完整的一份。ZIP内按产品建立目录，不改写源文件。

估值区间边界是否为交易日，以本地Wind白名单A股指数行情日期并集判断。请求日出现在交易日历中时，产品必须具有当天估值才满足边界完整；日历覆盖区间内未出现的日期视为休市日，可向前取最近估值。超出日历覆盖范围时，仅周末按非交易日处理，工作日从严要求当天估值。禁止再以“全部产品当天是否都有或都没有估值”反推交易状态。

投资总览中的FOF1证投组拆分为专户组合、策略组合和全委组合。全委组合固定为尊享FOF55号、元盈FOF1号、3791号、8075号、11518号、11598号、5588号、君得3640、聚智多策略9号和嘉盈1号，并使用顶层FOF净资产、收益和台账投资额。天玑13号、源泉优享FOF3号和盛乾同行2号按截止日前最近估值表穿透：“形式=专户”的在投持仓归专户，其余在投持仓归策略，砚博除外。底层数量按FOF与持仓代码分别计数；期末金额取实际底层市值，当前投资额取底层成本，区间收益取可归属估算收益。专户和策略的可计算分子必须同时满足所属FOF区间边界完整且底层收益可归属，分母仍是截止日在投持仓数。FOF1净资产扣除全委顶层净资产、专户底层市值和策略底层市值后的余额，以“未穿透差额（现金及净应收应付）”二级子行展示，主要反映三只穿透FOF的现金、应收应付及费用净额，不做比例分摊；该余额不是持仓或期间收益，因此收益、数量和投资成本列显示为“—”。

“报告出具”沿用独立月报生成器：输入截止日，月度起点取上月最后一个日历日，终点取输入日期；边界没有估值时向前取最近估值。顶层金额收益、Modified Dietz收益率、财年收益和底层可比持仓估算均沿用本文件既有口径。报告只读取本地估值表和资金台账，不更新邮件、行情或网页缓存。

底层标签依次采用《管理人清单》“策略归类拆分”和《产品标签》。匹配顺序为标准化完整名称、管理人一致的简称、唯一简称；并列冲突、简称过短和未匹配均归为“其他”。多策略不按描述比例拆分。

### 标签来源及字段映射

“策略组合分析（全部FOF底仓）”按匹配结果中的 `primary`（有效一级标签）汇总，不直接使用二级标签，也不直接使用未经转换的原始一级标签。字段映射如下：

| 来源 | 工作表/范围 | 原文件字段 | 程序字段 | 用途 |
|---|---|---|---|---|
| 《管理人清单.xlsx》 | `策略归类拆分`，从第3行开始 | B列“管理人名称” | `manager` | 简称匹配时校验管理人 |
| 同上 | 同上 | I列“所投产品名称” | `product` | 与估值表底层产品名称匹配 |
| 同上 | 同上 | J列“所投具体策略” | `classification_evidence` | 股票中性/对冲细分依据 |
| 同上 | 同上 | K列“投资形式” | `vehicle` | 专户/集合；缺失为“其他” |
| 同上 | 同上 | CD列“一级标签” | `raw_primary` | 原始一级标签 |
| 同上 | 同上 | CE列“二级标签” | `secondary` | 持仓分层及底层基准映射 |
| 《产品标签.xlsx》 | 各策略工作表，从第2行开始 | “管理人” | `manager` | 简称匹配时校验管理人 |
| 同上 | 同上 | “产品名称” | `product` | 与估值表底层产品名称匹配 |
| 同上 | 同上 | “一级标签”；为空时使用工作表名 | `raw_primary` | 补充原始一级标签 |
| 同上 | 同上 | “二级标签” | `secondary` | 补充二级标签 |
| 同上 | 同上 | “是否有敞口”（仅存在该列时） | `classification_evidence` | 股票中性/对冲细分依据 |

来源优先级为《管理人清单》高于《产品标签》。《管理人清单》已投产品记录匹配成功时优先保留其一级、二级标签和专户/集合；只有其原始一级标签为“其他”，并且《产品标签》对同一产品仅有一个不冲突的一级/二级组合时，才用《产品标签》补齐一级、二级标签，投资形式仍沿用《管理人清单》。

名称匹配前会去除空格、标点、份额类别及“私募证券投资基金”等通用后缀。匹配顺序为：标准化完整名称 → 管理人一致的简称包含匹配 → 唯一简称包含匹配。简称主体须不少于4个汉字或6个字母数字；同分候选、标签组合冲突、简称过短或无匹配时，`primary`、`secondary`、`vehicle`统一回退为“其他”。

原始一级标签通过以下规则形成全局分析使用的有效一级标签 `primary`：原始标签不是“股票”时保持不变；原始标签为“股票”时，先检查二级标签、所投具体策略和敞口说明，含“灵活对冲”“多空对冲”或“不完全对冲”归为“股票对冲”，否则含“中性”或“完全对冲”归为“股票中性”，其余归为“股票指增”。网页“产品标签”窗口同时保留原始一级标签、有效一级标签、二级标签、投资形式、来源、匹配方式和分类依据供核查。

底层价格序列取同一FOF各估值日、同一底层科目代码的市价/估值价格，属于估值价格代理而不是管理人正式累计单位净值。分红除权会使该价格代理机械下调，不能据此判断累计单位净值下跌。收益分析中单底层内联图以本地Wind白名单A股指数行情日期并集作为交易日历：周末和休市日不占横轴，缺失的正常交易日仍保留间隔；交易日历无法覆盖全部观测时回退真实日历轴并提示。区间收益率、年化收益率、夏普和最大回撤沿用顶层累计净值的不规则日期算法。卡玛比率为：

```text
卡玛比率 = 净值代理年化收益率 ÷ |最大回撤|
```

最大回撤为0或数据不足时不计算卡玛。底层Alpha/Beta仅在标签能映射Wind白名单基准时计算，仍要求至少10个有效相邻区间、跨度不少于30天且基准方差非零。中证2000、中证全指、中证A500、中证红利、国证2000使用 `AIndexEODPrices`；南华商品指数使用 `THIRDPARTYINDEXEOD`。

策略占比按所选结束日底层市值汇总一级标签；收益贡献按区间可归属估算收益汇总一级标签。退出成交价、现金、费用和部分已实现损益可能无法归属，因此底层合计不要求与FOF总收益完全勾稽。

原始一级标签“股票”进一步形成有效一级标签：含“中性”或“完全对冲”为股票中性；含“灵活对冲”“多空对冲”或“不完全对冲”为股票对冲；其他股票多头、指数增强和信息不足的股票策略为股票指增。原始标签和分类依据同时保留。

单只FOF的策略组合分析按有效一级标签汇总，表格仅展示期末市值、期末市值占比和区间可归属估算收益贡献，不再计算或展示“占FOF期初资产”。环形图使用相同的期末市值占比，正负柱状图使用相同的估算收益贡献，图表样式不引入新的计算指标。

全局策略组合分析将所选区间内17只FOF的底仓逐持仓累计，同名产品在不同FOF中不去重。市值占比的分母为全部底仓期末市值；盈利为各持仓区间可归属估算收益之和。砚博已包含在天玑13号持仓中，不再作为独立产品重复加入。

全局策略绝对收益率不使用全部FOF期初资产，也不再根据估算盈利反推投入本金。它使用期末仍持有且区间价格有效的底仓，按期末市值加权其估值价格代理区间收益率：

```text
单只底仓代理区间收益率 = 区间末估值价格 ÷ 区间初估值价格 - 1
策略绝对收益率 = Σ(单只底仓期末市值 × 单只底仓代理区间收益率) ÷ Σ有有效收益率的期末市值
```

可归属估算盈利继续作为金额指标单独展示，不参与绝对收益率反推。该口径仍会受到期内持仓变化影响，已退出持仓也不会进入期末市值加权收益率。网页按有效一级标签折叠列示底仓，并按净值代理区间收益率从高到低排列。

FOF持仓明细明确剔除“以公允价值计量且其变动计入当期损益的其他投资_其他投资”“私募理财产品”等会计汇总项目，也不展示成本列及对应基准/Alpha/Beta列；明细采用一级标签、二级标签合并单元格的矩阵形式。

网页展示按“总部 → 子组 → FOF → 策略 → 底层产品”组织。驾驶舱重构只调整导航、筛选、下钻与视觉呈现，不改变本文件定义的收益、风险、标签、敞口和现金流公式。Barra与交易归因在缺少证券级因子数据或底层真实交易流水时不计算。

## 固定模板报告

## 市场看板与月度ETF配置口径

指数年化波动率为区间日对数收益的样本标准差乘以 `sqrt(252)`，少于20个有效日不计算。贴水看板复用风控缓存，变化量是年化贴水率的百分点差，历史分位使用同品种同期限序列。

配置评分为 `35% 趋势 + 20% 宏观 + 20% 相对强弱 + 15% 情绪与波动 + 10% 估值/风格`；每项当期横截面标准化并截断到±3。数据缺失的分量以中性0分明示，不虚构数值。入选指数按60日波动率倒数和评分倾斜分配，宽基合计不低于50%，单宽基/风格/行业上限分别为35%/15%/10%，月度单边换手不超过30%。宏观按实际发布日进入信号，月末信号在下一交易日生效，不用当前ETF回填历史。结果仅供研究，不是交易指令。

旧版右上角“生成报告”入口已经移除。管理层报告统一由左侧“核心汇报”提供，并沿用当前日期、范围、确定性计算口径及受保护的Excel下载和浏览器打印/PDF功能。

## 风控页面：指数VaR、股指期货贴水与压力测试

指数VaR使用2010年以来中证1000、中证500和沪深300官方连续点位的相邻日对数收益，按历史模拟法取收益左尾，展示90%、95%、99%置信度的一日VaR正数及按当前指数风险暴露折算的金额。指数不做个股式前后复权，也不切换为全收益指数。

10年期国债收益率来自RQData的中国市场中债国债收益率曲线，固定读取`10Y`期限和2010年以来数据。相邻日变动及历史模拟VaR口径为：

```text
日收益率变动(bp) = (当日10Y收益率 - 前日10Y收益率) × 10000
90% / 95% / 99% VaR = 日收益率变动对应置信水平的右尾分位数
```

收益率上行通常对应债券价格下跌，因此取右尾作为不利方向。页面同时展示最新收益率、样本区间和有效日变动数量。当前未读取债券持仓久期和利率风险敞口，故不折算金额，不把bp VaR描述为损失预测。RQData失败时保留上次成功缓存，并独立披露错误，不影响Wind指数和股指期货缓存更新。

IF、IC、IM分别对应沪深300、中证500和中证1000。每日将未到期合约按到期日排序为当月、下月、当季、下季：

```text
年化贴水率 = (现货指数 - 期货收盘价) / 现货指数 × 365 / 剩余日历天数
```

正数表示贴水、负数表示升水。贴水VaR使用年化贴水率的相邻日变化计算历史左尾不利变化，单位为百分点；不把贴水水平作为VaR。IF从2010年开始，IC和IM从实际上市日开始，不补造历史。

压力测试取每只FOF最新估值表中的底层期末市值，同名底层在不同FOF中分别计入。固定系数为：股票指增对应指数1.0；股票中性年化贴水1.0；股票对冲对应指数0.5加年化贴水0.5；CTA/商品南华商品1.0；宏观、多策略、套利、可转债、打板和其他不估算。中证2000、国证2000、中证A500、中证全指和中证红利没有直接期货，统一使用IM贴水代理。

六个固定情景为：股市温和下跌（指数-5%、商品-10%、贴水扩大5个百分点）、股市严重下跌（-10%、-10%、扩大10个百分点）、股市温和上涨（+5%、+10%、贴水收窄5个百分点）、股市强劲上涨（+10%、+10%、收窄10个百分点）、贴水单独扩大10个百分点和贴水单独收窄10个百分点。持仓模拟收益等于期末市值乘各固定系数与情景因子变化之和。结果不包含波动率、相关性突变、流动性、费用和非线性风险，不构成损失预测或交易建议。
## 底层资产多头敞口

“底层分析”独立读取项目根目录 `底层资产` 中的底层产品估值表。多头敞口定义为：

`股票市值 + 股指期货多头名义市值 - 股指期货空头名义市值`

股票市值使用科目代码 `1102` 的汇总市值。股指期货只读取名称包含“股指期货初始合约价值”的多头或空头汇总科目；排除冲销科目、估值增值科目及合约叶子行，避免重复累计。期货多空名义金额统一取绝对值后按方向加减。页面同时列示关联FOF、估值日期、净资产占比和逐项依据；未匹配FOF不影响敞口计算。
## 底层资产多头敞口

“底层分析”独立读取项目根目录 `底层资产` 中的底层产品估值表。公式为 `股票市值 + 股指期货多头名义市值 - 股指期货空头名义市值`。股票使用 `1102` 汇总市值；股指期货只读取“股指期货初始合约价值”多头或空头汇总科目，排除冲销、估值增值和合约叶子行。期货金额取绝对值后按方向加减。页面列示关联FOF、估值日期、净资产占比及逐项依据。
## 风控日报全资产简化VaR

该计算在网页“其他管理”中独立展示，与FOF底层风控分开。默认每日`refresh`、市场`risk`及网页`build`均不重新解析日报或重算，只读取上次缓存；执行`python app.py portfolio-var`才主动更新日报VaR并重建网页和下载Excel。

每日风控日报中的权益FOF采用固定5%：`VaR=日报产品本金×5%`。三个利率专户只使用底层投资明细，避免与产品层本金重复。

境内固收采用：`利率VaR=敞口代理×代理修正久期×中债10Y的99%日上行VaR(bp)/10000`；信用附加风险采用相同久期乘以固定冲击。代理久期按剩余期限分为≤1年0.5、1–3年2、3–5年4、>5年6。固定信用冲击为AAA/AAAsf 3bp、AA+/AA 5bp、A+/A 8bp、NR或缺失10bp、次级/劣后及信托12bp，重叠时取高档。

美元固收按最近标准期限的美国国债99%日上行VaR计算；缺少有效久期时Agency MBS使用4年，其他海外ABS使用3年，信用附加分别为5bp和10bp。美元资产默认未对冲，USD/CNY下跌造成的人民币价值损失取历史99%分位并线性加入。

RQData美国国债接口的真实返回可能使用百分数值（如4.70代表4.70%）；程序按整条美国国债10Y历史曲线的量纲统一转换为小数，不能逐点按是否大于1判断，以免低于1%的历史收益率产生100倍单位错误。全部美元利率资产统一使用美国国债10Y收益率上行VaR，不按资产剩余期限匹配其他美债期限。

碳资产统一使用本地《碳排放价格》Excel中单一价格序列的涨跌幅作为管理代理，不再按碳市场或品种分别匹配：`VaR=数量×Excel最新价格×统一碳价代理历史日跌幅99%分位`。风险页把股票指数、中美国债各期限、USD/CNY和碳价的历史VaR集中展示，并保留百分比与bp的原始单位。网页仅展示逐资产明细，完整参数同步导出为`全资产VaR明细.xlsx`。各风险分量及各资产VaR均线性相加。结果统一称为“简化估算VaR”，日报金额仅为敞口代理，不是实时市值、正式监管VaR或损失预测。
