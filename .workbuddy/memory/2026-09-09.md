# 2026-09-09 工作日志

## M0-M5 大型升级全部完成（策略实验室独立化 + 全站 UI 统一）

- **M0 基建**：gh CLI 2.97.0 安装；taste-skill + redesign-skill 装入 ~/.workbuddy/skills/；GitHub milestones #6-#10 创建并随完成关闭；worktree 布局 D:/ai_projects/mail-evaluation-worktrees/（junction 链接主仓 products/market_data/data_sources/knowledge_base）
- **M1 ETF 品种扩大**：候选池 7→10 指数键 + 18 申万一级行业 ETF；etf_loader 单只失败降级（分支 feature/m1-etf-expansion）
- **M2 研究工作流**：strategy_lab 新增 factor_analysis（相关矩阵/VIF/RankIC）、sweep（alpha/训练月数网格）、attribution（资产+因子归因）（分支 feature/m2-research-workflow）
- **M3 LLM 双通道**：strategy_lab/llm/ 包（provider 云端OpenAI兼容+ollama 本地、spec schema 校验、generate 重试+兜底、executor 四方法论）；web.py 任务接口 POST /api/strategy-lab/llm + GET /api/strategy-lab/task/<id>（分支 feature/m3-llm-dual）
- **M4 独立界面**：DASH_PAGES 新增 lab 页四步工作流（数据→因子→策略→回测）+ LLM 面板，taste-skill 设计规范（分支 feature/m4-lab-ui）
- **M5 汇总+UI统一**：核心汇报申购记录按产品汇总（网页+Excel 五列）；全站调色板统一（米色系→蓝灰系）、.value 污染修复、按钮 5 色→2 色（分支 feature/m5-core-ui）
- main 已合并推送（4192578），168 测试全绿
- LLM 凭据模板在 README（LLM_API_BASE/LLM_API_KEY/LLM_MODEL/OLLAMA_URL/OLLAMA_MODEL）

## 环境坑（重要）
- 本机 git 2.55 有 bug：`git branch` 写 ref 丢失（refs/heads/feature/ 目录自动消失）→ workaround 是手动 printf 写 ref 文件，提交 hash 从 .git/worktrees/<wt>/logs/HEAD 提取
- gh 设备授权网络不稳（token 交换反复 EOF/BadGateway）→ 用 `git credential fill` 取凭据管理器 token + GH_TOKEN 环境变量调 gh api
- 测试要用系统 Python 3.10（C:/Users/admin/AppData/Local/Programs/Python/Python310/python.exe），managed Python 3.13 缺 openpyxl/rqdatac 依赖

## 网页白屏修复（10:30-11:00）

- 根因：M4 合并引入两处 JS 语法错误——①dashGoCore 路由 `renderStrategyLab()` 后缺分号紧跟 `const`；②renderStrategyLab 函数末尾多一个 `}`。app.js 无法编译 → 浏览器白屏
- 定位方法：node `new Function(code)` 逐行累加编译 + vm.Script 定位行号 + 括号深度追踪脚本（跳过模板字符串）
- 修复：static.py 改 2 处 → app.py build 重建 → 服务器实测 app.js 编译 OK、全链路 200
- 教训：**static.py 是巨型单行 JS 字符串，改完必须先跑 node 语法检查再 build**——已建议加入测试（test_static 可加 JS 编译断言）
- 知识库上传功能验证正常：POST /api/knowledge/upload（Base64）→ KnowledgeStore SHA-256 去重落盘 knowledge_base/files/，全文检索 OK，DELETE 走回收区
- 修复已提交 main（2937b6d）并推送

## JS 语法测试 + 真正的全站视觉升级（11:05-11:45）

- 用户反馈"UI 升级看不出来"——事实正确：M5 只做了技术债清理（色值统一），未做视觉重塑
- 本轮按 taste-skill 规范完成 18 项视觉升级：背景双层径向渐变、卡片渐变+双边阴影、KPI 顶部强调线、全局 tabular-nums、品牌区/导航单主色（#1ca082 绿青）、表格行悬浮、按钮按压反馈、顶栏毛玻璃 saturate、侧栏内顶光、输入框 focus ring、warn/ok 统一圆角
- 新增 StaticJSSyntaxTest：node --check 编译 DASHBOARD_JS，已验证能拦截 M4 类缺分号错误；169 测试全绿
- 提交 c3f5211 已推送 main
- 经验：视觉升级要"看得出来"必须动 surface（背景/阴影/强调线/悬浮态），只统一变量值用户无感

## 天玑13号勾稽失败诊断（12:12）

- 现象：网页提示"台账累计净投资与当前总投资额不一致，暂不计算收益"
- 数据事实：台账流水净额 77,600万（含 2026-09-07 的 -3,000万 赎回）；余额表(专户资金余额2026) 本金规模 80,600万 未扣该笔
- 9/8 发布的估值表（估值日 9/7）已体现赎回：资产净值成本口径 84,475万→81,475万（-3,000.6万），市值口径 80,589万
- 结论：**不是缺估值表，是余额表快照落后于台账**。用户起初以为缺 9/8 估值表——已澄清：9/8 估值表已到且已进网页（build 10:23 晚于文件 10:16）
- 同批问题产品：源泉优享FOF3号 9/7 也有 -800万 赎回（余额表 64,900万 应为 64,100万）
- 修复路径：Excel 更新余额表两行 → python app.py refresh；或等余额表例行更新

## 五项需求批量处理（12:12-13:00）

1. 台账勾稽验证：用户改完余额表后全产品 0 不一致，天玑13号收益恢复；网页已重建
2. 知识库元数据修正：类型识别改文件名优先（报告类不再误判"产品合同"）；机构正则收紧排除免责声明；存量8份文档重推+文件名前缀补机构
3. 导航重构：产品标签→左侧独立页（策略组合前）；策略实验室→系统与数据组；改动必须落在 DASHBOARD_JS r''' 主块（HTML 内嵌副本和 replace 链有重复定义，之前改错过一次）
4. 策略实验室：四步工作流可点击+连接箭头；五步端到端操作案例卡片（可折叠）
5. 经验：static.py 的 DASHBOARD_JS 主块 vs HTML 内嵌版 vs .replace() 猴补丁链三处重复，改导航类功能必须改主块并核对产物 app.js
