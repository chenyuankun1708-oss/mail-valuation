# AGENTS.md

## 项目目标

本仓库围绕“邮件估值表下载 → 字段解析 → 单一投资人收益计算 → Wind指数基准分析 → 底层标签与策略组合分析 → 风控日报全资产简化VaR → 本地网页展示”维护。行情仅允许通过Wind Oracle数据库读取以下白名单：中证1000（000852.SH）、中证500（000905.SH）、沪深300（000300.SH）、中证2000（932000.CSI）、中证全指（000985.CSI）、中证A500（000510.CSI）、中证红利（000922.CSI）、国证2000（399303.SZ）和南华商品指数（NH0100.NHF）。RQData例外严格限于中国市场10年期中债国债收益率曲线、美国国债10年期收益率曲线和USD/CNY人民币参考汇率；不得开放任意米筐代码、期限、证券或其他接口。风控仅允许增加IF、IC、IM股指期货及其当月、下月、当季、下季年化贴水，不开放任意期货查询。前三只指数用于顶层展示和历史模拟VaR，全部指数白名单可用于底层标签对应基准；除只读解析每日风控日报中列明的资产并执行本文确定的简化VaR外，禁止其他公网行情接口、任意代码查询、其他行情爬虫及任意证券组合分析。

## 不变量

- 市场研究看板是 RQData 的受限例外：仅允许固定的 PMI/CPI/PPI/M2/工业增加值、中美10年国债、USD/CNY、全部申万一级行业指数、Size/Momentum/Book-to-Price/Earnings Yield/Growth/Liquidity/Residual Volatility 因子收益、IF/IC/IM、CU/AU/RB/SC/M/P、50ETF/300ETF/500ETF/中证1000期权及清单中的ETF候选池。不得在网页开放任意代码、标的、宏观指标或商品品种查询；权限缺失必须回退到单模块缓存并显示缺失。

- 允许在限定的底层资产目录内进行证券级多因子暴露分析：仅解析A股叶子持仓并读取Wind `QUANTFACTORWIND`九类基础因子和Wind行业有效期映射。页面必须称为“自研多因子暴露分析”，不得称为MSCI Barra正式模型，也不得虚构协方差、特异风险、跟踪误差或正式风险贡献。

- 产品白名单只在 `valuation_app/config.py` 维护，修改产品名称时同步补充稳定别名或产品代码。
- 投资总览FOF1子组合采用混合口径：全委组合固定为10只顶层FOF；专户和策略组合只穿透天玑13号、盛乾同行2号、源泉优享FOF3号的截止日在投底层。底层“形式=专户”归专户，其余归策略；砚博继续从FOF1扣除并归其他组。同一底层跨FOF持有必须按持仓分别计数和汇总，不得硬编码专户数量或虚构未匹配标签。
- FOF1净资产扣除全委顶层净资产、专户底层市值和策略底层市值后的余额，必须作为FOF1下“未穿透差额（现金及净应收应付）”二级子行动态展示，主要反映三只穿透FOF的现金、应收应付及费用净额；不得把该余额计作持仓、区间收益、产品数量或投资成本。
- 专户及策略组合的“可计算持仓数”必须同时满足所属FOF的所选区间边界完整、且该底层收益可归属；分母仍为截止日在投持仓数。持仓明细只在收益分析展示，不设置重复的独立持仓页面。单底层内联曲线使用本地Wind A股交易日历压缩非交易日，并保留缺失交易日间隔。
- 顶层产品及FOF1穿透组合的估值边界必须使用本地Wind白名单A股指数行情日期并集判断交易日；交易日要求请求日当天估值，非交易日才允许向前取值。日历覆盖外的工作日从严按交易日处理，不得根据产品当天是否普遍缺少估值反推休市。
- 投资总览的“FOF产品收益计算”只列示配置中的17只顶层FOF，不重复纳入砚博或其他底层持仓。当前范围默认全选，日期重算或范围切换后重置为全选；勾选仅影响本表会话内合计，不得联动KPI、图表、报告或其他页面。日均占用资金使用台账本金余额按自然日加权，现金流发生日生效；组合XIRR必须合并实际日期现金流重新求解，不得平均单品XIRR。无法计算项保留但不进入有效合计，边界不完整项必须显示实际估值区间。
- 网页“代码指令”必须以统一宽度的分组卡片展示全部公开CLI命令；长PowerShell命令允许换行，不得撑破布局。新增、删除或改变 `app.py` 命令时必须同步更新该页面和README。
- 所有 `python app.py ...` 命令必须使用单调时钟输出步骤及总耗时：业务JSON保留在stdout，实时计时写stderr。`refresh`须分别披露邮件与文件、数据库与缓存、数据计算和网页更新耗时；18:30计划任务通过`scripts/refresh.ps1`将相同信息和脚本总耗时写入被Git忽略的`logs/refresh.log`，不得记录凭据。
- 公网分享默认使用Tailscale Funnel固定`*.ts.net`地址，Python服务仍只监听`127.0.0.1`并保留Basic Auth；开机任务必须以最高权限调用`start_tailscale_share.ps1`，确保能够访问本机Tailscale受保护管道，Cloudflare Quick Tunnel仅作临时备用。不得把含财务数据的网页发布到公开GitHub Pages；分享地址和运行日志继续由Git忽略。
- `start_tailscale_share.ps1`发现Tailscale重启后尚未进入Running状态时，必须先自动启动本机`tailscale-ipn.exe`并等待一次恢复，再决定是否报错；不得改变固定域名或绕过登录状态。
- 网页发布采用轻量入口HTML与受Basic Auth保护的只读数据接口；首屏JSON、脚本及按页面延迟加载的市场研究、多因子、底层资产、台账/标签工作簿和风控日报VaR模块必须启用gzip、ETag及私有缓存。模块路由只能使用代码登记的固定白名单，不得接受任意路径或文件名；访客浏览器不得直连Wind、RQData或邮箱。
- 网页入口每次构建必须使用`page_updated_at`为`app.js`增加版本参数，脚本响应须重新校验ETag，避免发布后浏览器在缓存期内继续显示旧功能。
- 原始估值表只读，不改写、不删除；下载以附件内容 SHA-256 去重。
- 首页单日估值表压缩包只允许读取`products`目录中所选日期实际存在的已配置顶层产品原始估值表，不得向前补取；同内容去重，同产品同日多版本保留可解析持仓更完整的一份。下载接口必须沿用Basic Auth并禁止任意路径输入。
- 不在源码、日志、测试或文档中写邮箱密码；凭据只从环境变量或被 `.gitignore` 忽略的 `.env` 读取。
- 现金流事件必须保留判断依据和置信度。不得把估值结果描述成已确认的交易流水。
- 收益计算必须显式处理申购、赎回和分红，禁止退化为“期末净值 / 期初净值 - 1”作为金额收益。
- 收益分析指标卡必须在“期末资产”前展示“期初资产”，并在“最大回撤”后以同样式展示“最大回撤区间”；区间格式固定为 `YYYY-MM-DD → YYYY-MM-DD`，缺失时显示“无法计算”，不得在卡片下重复展示同一说明。
- 网页“计算口径”必须为所有可计算指标逐项展示公式、变量含义、单位、年化方式、样本要求和缺失规则；分类、标签匹配、数据来源、日期边界与缓存回退等非公式项目必须明确称为“规则口径”，不得伪造数学公式。
- 新增估值表模板时，应优先增加解析测试，不要为单个文件名写一次性分支。
- 保持 Python 3.7 兼容，不使用外部前端构建链。
- 风控压力测试仅使用文档确定的固定六情景与透明固定系数，不得把线性模拟收益描述为损失预测或交易建议。
- 10年期国债收益率VaR只按日收益率上行变动的右尾计算并以bp展示；没有债券久期和利率风险敞口时不得折算金额或描述为损失预测。
- 风控日报全资产VaR只能称为“简化估算VaR”：权益FOF固定5%，固收使用透明代理久期和固定信用利差冲击，美元资产默认未对冲，碳资产只读本地《碳排放价格》历史序列并以该单一涨跌幅序列统一代理全部碳资产；日报金额必须标记为敞口代理，不得称为实时公允市值、监管VaR或损失预测。
- 风控日报产品层与底层不得重复计算；国泓、长盛汇利2号和长盛海昇5号只使用底层明细。组合汇总采用逐项VaR线性相加，并披露覆盖率、未计算敞口、凸性、提前偿还、流动性和违约风险等边界。
- 风控日报全资产VaR必须在网页“其他管理”独立展示，不得混入FOF底层风控；默认`refresh`、`risk`和`build`不得重算，只有显式`portfolio-var`命令可以解析日报、更新缓存并重建网页和Excel。
- 《产品标签.xlsx》和《管理人清单.xlsx》仅允许由 `labels-migrate` 一次性只读迁移；迁移后 `data_sources/product_labels.json` 是唯一运行时权威源，原Excel只作历史备份，不得参与刷新或计算。简称匹配存在歧义时必须归入“其他”，不得靠产品名称猜测策略。
- 标签查询、创建、修改和停用接口必须沿用Basic Auth，只允许固定JSON路径与记录ID；写入必须校验全局修订号并以临时文件原子替换，修改前自动备份，审计记录用户、产品及修改前后字段但不得记录密码或认证头。
- 网页标签编辑器必须支持搜索、启用状态筛选、新增、修改和停用；保存后必须重新获取服务端匹配结果并即时刷新标签、策略组合及FOF1专户/策略分类，不得要求重建网页。未匹配或歧义项继续归入“其他”并显著提示。
- 核心汇报必须使用全局起止日和分析范围，以分段核心结论开头，完整列示全部顶层产品台账申购赎回；底层分析严格限于盛乾同行2号FOF、源泉优享FOF3号、天玑13号和西南证券嘉盈1号FOF。确定性规则识别勾稽、收益贡献/拖累、集中度、底层持仓增减估算、底层及策略收益率高低和风险异常。核心汇报不得展示数据质量提示；底层增减不得描述为真实交易或申赎，不得调用大模型、汇总“其他管理”VaR或生成投资建议。旧版“生成报告”按钮不得显示，管理层导出统一由核心汇报提供。
- 左侧“核心汇报”必须保持独立菜单但不得替代默认“投资总览”，页面按摘要、重点、证据分层；Excel导出和浏览器打印/PDF必须沿用当前日期与范围，下载接口受Basic Auth保护并限制固定字段和明细规模。
- 本地知识库原文件、SQLite数据库、索引、inbox、回收区和导出元数据统一保存在被Git忽略的 `knowledge_base`；文件按SHA-256去重。网页只允许上传、访问固定文档ID及导入固定 `knowledge_base/inbox`，不得接受任意本机路径。所有知识库接口沿用Basic Auth；删除必须移入回收区而非物理删除。扫描件无文本层时保留原文件并明确提示需OCR，不得伪造正文；当前不得调用大模型或主动抓取公网/微信内容。
- “持仓变动归因（估算）”只允许用相邻估值表按 `ΔMV=q0×(P1-P0)+(q1-q0)×P1` 拆分原持仓价格影响和仓位变化金额；新增按期末市值、清仓按负的期初市值估算并标记部分可归属。顶层产品区间收益扣除可归属价格影响后的差额必须单列为现金、费用、交易价格、退出损益及其他未归属项。不得反推缺失成交价、交易费用或把仓位变化金额称为交易盈利；页面与Excel必须保留FOF维度，不得跨FOF去重。
- `strategy_lab` 必须保持可整体迁出的独立研究包，只消费市场看板已落盘的固定白名单缓存，不在网页接受任意代码、接口或文件路径。因子须记录可用时间，Ridge只用截至信号日已实现目标的扩展训练样本，至少36个月，标准化参数只来自训练窗；月末形成信号、下一交易日模拟执行。组合必须无杠杆、无做空、100%投入，宽基不低于50%，风格及行业各不超过30%，单只宽基/风格/行业分别不超过35%/15%/10%，单边换手不超过30%。输出仅为版本化研究JSON、走步回测和建议权重，不得连接券商、生成真实订单或保存交易凭据；主项目只读消费结果。LLM双通道（云端OpenAI兼容API+本地ollama）只输出受schema校验的策略spec（方法论/参数范围/universe白名单），本地解释执行并强制组合约束，绝不执行LLM生成的代码；凭据只从.env读取（LLM_API_BASE/LLM_API_KEY/LLM_MODEL/OLLAMA_URL/OLLAMA_MODEL），不进源码、日志或测试；运行版本化落盘strategy_lab_data/llm_runs/。
- 底层风险指标使用估值表市价作为净值代理时，页面必须明确说明不是管理人正式累计单位净值。
- 单只FOF策略组合表仅展示有效一级标签、期末市值、期末市值占比和区间可归属估算收益贡献，不展示“占FOF期初资产”；策略环形图与收益贡献柱状图只能改变呈现，不得另造计算口径。
- 收益算法、日期边界、现金流和台账口径、产品范围或分组、估值表解析、底层穿透、网页分享与安全机制、运行命令或自动刷新流程发生重大变化时，必须同步更新网页“项目说明”的相关栏目及“历史改动”，并更新对应说明文档和测试；不得只改代码。
- 每次代码或功能的重要改动必须在同一个提交中完成“五处同步”：①网页“数据与说明”中的相应计算口径、操作方法、数据边界或项目资料；②网页“历史改动”，日期采用实际变更日并保持从近到远；③本文件 `AGENTS.md` 中受影响的项目目标、不变量、验证命令或维护规则；④根目录 `README.md` 及必要的专项说明文档；⑤可提交的 `logs/CHANGELOG.md`。缺少任一项不得视为完成。
- `logs/CHANGELOG.md` 只记录可审计的代码与功能变更，按日期从近到远排列，不得写凭据或敏感数据。`logs/refresh.log`、`logs/cloudflared.log`、分享服务日志等属于本机运行日志，继续由Git忽略，不得因记录版本历史而提交或改写巨型运行日志。
- “重要改动”至少包括：新增或移除功能、页面入口或交互变化、计算公式或数据口径变化、产品及组合分组变化、解析规则或数据源变化、接口与命令变化、安全和权限变化、自动任务变化、缓存结构变化以及会影响用户理解或结果复现的缺陷修复。纯格式化、注释或不改变行为的内部重构可只写 `logs/CHANGELOG.md`；但用户可见样式变化仍须更新网页历史改动。

## 验证

提交变更前运行：

```powershell
python -m unittest discover -s tests -v
python app.py analyze
```

解析错误应出现在 JSON 的 `parse_errors` 中；无数据产品应保留在页面并显示缺失状态。
- `底层资产` 文件夹中的估值表只用于底层敞口分析；股票取1102汇总市值，股指期货只取初始合约价值汇总科目，必须排除冲销、估值增值和合约叶子行，防止重复计算。
- `底层资产` 估值表只用于底层敞口分析；股票取1102汇总市值，股指期货只取初始合约价值汇总科目，必须排除冲销、估值增值和合约叶子行。
- 指数同期表现仅展示中证1000、中证500、沪深300和中证2000；其他白名单指数只用于底层标签基准或风控计算，不进入顶部同期表现。
