# AGENTS.md

## 项目目标

本仓库围绕“邮件估值表下载 → 字段解析 → 单一投资人收益计算 → Wind指数基准分析 → 底层标签与策略组合分析 → 风控日报全资产简化VaR → 本地网页展示”维护。行情仅允许通过Wind Oracle数据库读取以下白名单：中证1000（000852.SH）、中证500（000905.SH）、沪深300（000300.SH）、中证2000（932000.CSI）、中证全指（000985.CSI）、中证A500（000510.CSI）、中证红利（000922.CSI）、国证2000（399303.SZ）和南华商品指数（NH0100.NHF）。RQData例外严格限于中国市场10年期中债国债收益率曲线、美国国债10年期收益率曲线和USD/CNY人民币参考汇率；不得开放任意米筐代码、期限、证券或其他接口。风控仅允许增加IF、IC、IM股指期货及其当月、下月、当季、下季年化贴水，不开放任意期货查询。前三只指数用于顶层展示和历史模拟VaR，全部指数白名单可用于底层标签对应基准；除只读解析每日风控日报中列明的资产并执行本文确定的简化VaR外，禁止其他公网行情接口、任意代码查询、其他行情爬虫及任意证券组合分析。

## 不变量

- 允许在限定的底层资产目录内进行证券级多因子暴露分析：仅解析A股叶子持仓并读取Wind `QUANTFACTORWIND`九类基础因子和Wind行业有效期映射。页面必须称为“自研多因子暴露分析”，不得称为MSCI Barra正式模型，也不得虚构协方差、特异风险、跟踪误差或正式风险贡献。

- 产品白名单只在 `valuation_app/config.py` 维护，修改产品名称时同步补充稳定别名或产品代码。
- 原始估值表只读，不改写、不删除；下载以附件内容 SHA-256 去重。
- 不在源码、日志、测试或文档中写邮箱密码；凭据只从环境变量或被 `.gitignore` 忽略的 `.env` 读取。
- 现金流事件必须保留判断依据和置信度。不得把估值结果描述成已确认的交易流水。
- 收益计算必须显式处理申购、赎回和分红，禁止退化为“期末净值 / 期初净值 - 1”作为金额收益。
- 新增估值表模板时，应优先增加解析测试，不要为单个文件名写一次性分支。
- 保持 Python 3.7 兼容，不使用外部前端构建链。
- 风控压力测试仅使用文档确定的固定六情景与透明固定系数，不得把线性模拟收益描述为损失预测或交易建议。
- 10年期国债收益率VaR只按日收益率上行变动的右尾计算并以bp展示；没有债券久期和利率风险敞口时不得折算金额或描述为损失预测。
- 风控日报全资产VaR只能称为“简化估算VaR”：权益FOF固定5%，固收使用透明代理久期和固定信用利差冲击，美元资产默认未对冲，碳资产只读本地《碳排放价格》历史序列并以该单一涨跌幅序列统一代理全部碳资产；日报金额必须标记为敞口代理，不得称为实时公允市值、监管VaR或损失预测。
- 风控日报产品层与底层不得重复计算；国泓、长盛汇利2号和长盛海昇5号只使用底层明细。组合汇总采用逐项VaR线性相加，并披露覆盖率、未计算敞口、凸性、提前偿还、流动性和违约风险等边界。
- 风控日报全资产VaR必须在网页“其他管理”独立展示，不得混入FOF底层风控；默认`refresh`、`risk`和`build`不得重算，只有显式`portfolio-var`命令可以解析日报、更新缓存并重建网页和Excel。
- 《产品标签.xlsx》和《管理人清单.xlsx》只读；简称匹配存在歧义时必须归入“其他”，不得靠产品名称猜测策略。
- 底层风险指标使用估值表市价作为净值代理时，页面必须明确说明不是管理人正式累计单位净值。
- 收益算法、日期边界、现金流和台账口径、产品范围或分组、估值表解析、底层穿透、网页分享与安全机制、运行命令或自动刷新流程发生重大变化时，必须同步更新网页“项目说明”的相关栏目及“历史改动”，并更新对应说明文档和测试；不得只改代码。

## 验证

提交变更前运行：

```powershell
python -m unittest discover -s tests -v
python app.py analyze
```

解析错误应出现在 JSON 的 `parse_errors` 中；无数据产品应保留在页面并显示缺失状态。
- `底层资产` 文件夹中的估值表只用于底层敞口分析；股票取1102汇总市值，股指期货只取初始合约价值汇总科目，必须排除冲销、估值增值和合约叶子行，防止重复计算。
- `底层资产` 估值表只用于底层敞口分析；股票取1102汇总市值，股指期货只取初始合约价值汇总科目，必须排除冲销、估值增值和合约叶子行。
- 指数同期表现仅展示中证1000、中证500、沪深300和中证2000；其他白名单指数只用于底层标签基准或风控计算，不进入顶部同期表现。
